# Git commit messages

1. Use the format `<tag>: <message>`.
2. Valid tags:
    - `feat` - new feature
    - `fix` - bug fix
    - `tests` - adding or updating tests
    - `docs` - documentation changes
    - `refactor` - code restructuring without behavior change
    - `chore` - maintenance tasks (deps, config, tooling)