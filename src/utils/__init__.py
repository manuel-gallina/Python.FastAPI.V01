"""Package for development utilities not used at runtime.

These utilities and helper function MUST NOT be used
as part of the main application code, but are intended
to simplify development and debugging tasks.

This package MUST NOT include testing utilities, which should
be placed under the dedicated `tests` package.
"""
